<h1>Listing ward_values</h1>

<table>
  <tr>
    <th>Recorded</th>
    <th>Ward</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ward_values.each do |ward_value| %>
  <tr>
    <td><%= ward_value.recorded %></td>
    <td><%= ward_value.ward %></td>
    <td><%= ward_value.value %></td>
    <td><%= link_to 'Show', ward_value %></td>
    <td><%= link_to 'Edit', edit_ward_value_path(ward_value) %></td>
    <td><%= link_to 'Destroy', ward_value, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ward value', new_ward_value_path %>
